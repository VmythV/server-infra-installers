# 脚本规范

- 一个软件一个独立 `install.sh`。
- 不设计统一总安装入口。
- 默认交互式安装，`ASSUME_YES=1` 或 `NON_INTERACTIVE=1` 才走无人值守。
- 任何破坏性操作不得默认执行。
- 所有步骤必须通过统一步骤包装函数执行并记录日志。
- 支持 `CN=1` 切换中国大陆可访问的软件源、下载地址和镜像。
- 支持通过环境变量覆盖关键路径、版本、镜像前缀。
